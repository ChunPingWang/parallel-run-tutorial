# ADR-004：噪音基線與比對策略

- **狀態**：已接受
- **日期**：2026-07-25

## 背景

新舊系統的回應必然存在「合理不同」：時間戳、UUID、流水號、主機名（容器是 Pod name）、版本號、JSON array 順序、快取命中差異、CDC 同步延遲。

若不先處理，假陽性會淹沒真實缺陷，導致：
- 一致率數字失去意義
- 團隊對報告失去信任，開始忽略告警
- 真實缺陷被埋在數千筆噪音中

## 決策

**Phase 0 先跑「舊系統 vs 舊系統第二實例」建立噪音基線；自動學習產生的 ignore_paths 一律需人工簽核後才可套用。假陽性率 < 1% 才進入 Phase 1。**

## 理由

舊 vs 舊的比對中，凡是仍然不同的欄位，**依定義就是噪音而非行為差異**。這給了一個客觀、可重現的基線，不需要靠人猜哪些欄位該忽略。此思路來自 Diffy，本案自建實作。

**為什麼自動候選必須人工簽核**：自動學習只看「差異出現的頻率」，無法區分「時間戳」與「因排序不穩定而每次都不同的金額欄位」。實測中 `$.data.transactions[*].amount` 就會被列為候選 —— 而金額正是真實缺陷最可能躲藏的地方。

因此工具的設計是：
- 高頻差異 → `ignore_paths` **候選**（不是結論）
- 低頻差異 → `*.review.json` 人工佇列
- 兩者都需簽核，且金額 / 狀態 / 錯誤碼類**一律不得**自動忽略

## 比對維度

| 維度 | 判定 |
|---|---|
| HTTP status | 不一致即 `STATUS_MISMATCH` |
| 業務錯誤碼 | 新應用不得出現舊系統沒有的 code |
| Response body | JSON 正規化後深度比對；非 JSON 走遮蔽後文字比對 |
| 指定 header | 白名單比對，避免 `Date`/`Server` 噪音 |

## 噪音處理手段

| 手段 | 用途 | 設定 |
|---|---|---|
| `ignore_paths` | 完全忽略欄位 | 支援 `$.a[*].b`、`$..key` |
| `mask_patterns` | 遮蔽 body 內嵌動態字串 | 時間、UUID、流水號 |
| `sort_arrays` | 消除順序不穩定 | 依指定 key 排序 |
| `numeric_tolerance` | 吸收 CDC lag | **Phase 3 必須歸零** |
| `ignore_status_pairs` | 已知的 status 對應變更 | 極少用，需簽核 |

## 後果

**正面**
- 一致率數字可信，Gate 判定有意義
- profile 為版本控管的設定檔，每次變更可追溯

**負面**
- 需額外建置 legacy 第二實例
- W2 一整週投入在「還沒開始測新系統」，需向管理層說明其必要性
- **profile 過度抑制真缺陷是最隱蔽的風險**（風險 R5）—— 緩解手段為每階段人工抽樣複核 30 筆
