# ADR-007：錄製檔個資遮蔽與保留政策

- **狀態**：已接受
- **日期**：2026-07-25
- **關聯**：ADR-001（採錄製重放即產生此議題）

## 背景

GoReplay 錄製檔包含**完整的 request/response body**，在銀行場景即包含帳號、身分證字號、交易金額、聯絡方式。這些檔案一旦離開正線主機，等同把生產資料複製到測試環境。

## 決策

1. **法遵事前書面核准**為 Gate 阻擋項（檢核表 T-02），核准內容須含保留天數與遮蔽要求
2. 錄製檔離開正線主機前**必須**執行 `sanitize-gor.py`
3. 遮蔽採**固定 SALT 的一致性雜湊**，而非隨機值
4. 錄製目錄權限 700、磁碟加密、設定保留天數自動清除
5. 遮蔽報告**零命中時不得放行**，須回頭校準規則

## 理由

**為什麼是一致性雜湊而非隨機遮蔽**：同一個帳號在新舊兩邊必須被遮成同一個字串，否則遮蔽本身就會製造 100% 的假陽性，讓比對完全失去意義。

**為什麼零命中不得放行**：零命中通常代表規則與實際資料格式不符（例如帳號格式為 `1234-5678-90` 而規則只匹配純數字），而不是資料真的沒有個資。此時放行等於未遮蔽輸出。

## 遮蔽範圍

| 類別 | 處理 |
|---|---|
| 身分證字號 | 一致性雜湊 → `ID_XXXXXXXXXX` |
| 手機 / Email | 一致性雜湊 |
| 信用卡號 | 一致性雜湊（規則順序須在帳號之前） |
| 帳號 | 一致性雜湊 |
| `Authorization` / `Cookie` header | 整段抹除 |
| meta 行（uuid） | **保留** —— 配對鍵，不含個資 |

## 後果

**正面**
- 錄製檔可安全移至測試環境進行離線分析
- 遮蔽後仍可比對，不影響 diff 有效性

**負面**
- SALT 必須保密且固定；換 SALT 會讓既有錄製檔無法與新檔比對
- 遮蔽規則需依實際資料格式校準，非開箱即用
- 若某欄位遮蔽規則誤匹配（例如把訂單編號當帳號），會造成該欄位無法比對 —— 需在 Phase 0 一併檢視

## 保留政策

| 項目 | 建議 |
|---|---|
| 正線主機原始錄製檔 | 遮蔽並移出後**立即刪除**，最長不超過 24 小時 |
| 遮蔽後錄製檔 | 依法遵核准天數，建議 ≤ 30 天 |
| 比對報告（含差異樣本） | 樣本值已截斷至 200 字元，仍須依同等級管理 |
| PoC 結束 | 檢核表 C-01：確認含備份全數清除 |
